Mobile polish for 21 more apps + pinned Quick Tools + app ratings

MOBILE POLISH (21 apps):
Added targeted grid overrides for app-specific CSS class names not covered
by the baseline media query block. Each fix uses the right column count:
  form/input grids: 1fr (single column, full-width inputs)
  stats/results cards: 1fr 1fr (pairs of small cards)
  game/functional grids left unchanged

Apps fixed: break-even-calculator, compound-interest, depreciation-calculator,
ip-info, number-guessing, okr-tracker, prime-factorizer, qr-generator,
roi-calculator, sleep-calculator, statistics-calculator, unit-converter,
word-counter, html-entity-encoder, http-status-codes, pdf-to-image,
periodic-table (modal-grid only), color-picker, color-gradient-generator,
invoice-generator, text-case-converter.

USER-PINNED QUICK TOOLS (launcher):
The Quick Tools shelf now shows user-pinned apps instead of a hardcoded list.
Every app card has a pin button in the footer (star icon, toggles on/off).
  - Pins stored in localStorage (jorres_pinned_v1)
  - When using custom pins a Reset button restores the 6 defaults
  - Max 12 pins (oldest drops off if exceeded)
  - Shelf shows a hint explaining how to pin tools
  - Fully backward-compatible: users with no pins see the same defaults

APP RATINGS (launcher):
Each app card now has a thumbs-up button in the footer.
  - Clicking marks the app as helpful; button turns green after clicking
  - Count displayed if marked helpful more than once
  - Ratings stored in localStorage (jorres_ratings_v1)
  - Local-only, no server needed

17/17 smoke tests passing.
