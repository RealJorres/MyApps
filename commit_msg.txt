Audit 4 full remediation: all three tiers

TIER 1 - High-value fixes

SEO (T1.1): Added og:title, og:description, twitter:card to 38 apps that
were missing Open Graph meta. Meta description added to 35 apps. All real
content apps now have full social sharing metadata. Redirect/retired pages
correctly skipped.

Data portability (T1.2): Added exportData()/importData() and a Data card
to 6 remaining stateful apps: journal, habit-tracker, todo-list,
sticky-notes, calorie-tracker, pomodoro-timer. All 10 stateful apps
now have data portability.

Modal focus trapping (T1.3): Added _trapFocus()/_releaseFocus() helpers to
5 apps with modals that let Tab escape to background content: markdown-editor
(#helpModal), periodic-table (#modalOverlay), wordle (.modal-overlay),
flashcards (#editModal), kanban-board (#modalOverlay).

Aria-labels (T1.4): Added aria-label to 2 truly icon-only buttons:
api-tester kv-remove, pdf-merger file-remove.

Retired paths (T1.5): Added /percentage-calculator/ to robots.txt Disallow.

Code quality (T1.6): Fixed 2 img tags missing alt="" in image-comparison.

TIER 2 - Moderate effort

Duplicate CSS (T2.7): Stripped exact-duplicate .btn-primary and .btn-danger
rules from 55 apps. These were redundant with base.css via the
--brand: var(--primary) bridge. base.css is now the authority for button colors.

Mobile baseline (T2.8): Added @media (max-width:600px) block to 17 apps
that had zero responsive CSS. Every app now has at least baseline mobile
responsiveness.

Launcher skip link (T2.9): Added skip link and id="main-content" to the
launcher so keyboard users can skip nav to main content.

TIER 3 - Architecture

Gunicorn threads: Increased from 4 to 8 threads. 8 concurrent requests
within the single process (memory footprint unchanged - single worker).

Mind Map double-tap: Already implemented in a prior session. No change needed.

17/17 smoke tests passing.
